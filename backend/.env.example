# JWT
# 256-bit 이상 Base64 인코딩 문자열
# 생성: openssl rand -base64 32
JWT_SECRET=your-base64-encoded-secret-key-here

# Database (운영)
DB_USERNAME=stockuser
DB_PASSWORD=stockpass

# Redis (운영)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
